# Nodered Enwickungsumgebung
```
mpm -g nodered
mkdir node-red-dev
cd node-red-dev
npm init -y
node-red
erreichbar über http://localhost:1880

npm install -g node-red-dashboard
npm install -g node-red-contrib-modbus
npm install -g node-red-contrib-opcua
```